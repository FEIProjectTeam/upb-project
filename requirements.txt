asgiref==3.7.2  # Your version has priority
black==23.10.0
cffi==1.16.0  # Your version has priority
click==8.1.7
cryptography==41.0.4  # Your version has priority
Django==4.2.6  # Your version has priority
djangorestframework==3.14.0
mypy-extensions==1.0.0
packaging==23.2
pathspec==0.11.2
platformdirs==3.11.0
psycopg2-binary==2.9.9
pycparser==2.21  # Same, so no issue
pytz==2023.3.post1  # Your version has priority
sqlparse==0.4.4  # Your version has priority
certifi==2022.9.24  # From external, no conflict
charset-normalizer==2.1.1  # From external, no conflict
defusedxml==0.7.1  # From external, no conflict
idna==3.4  # From external, no conflict
oauthlib==3.2.2  # From external, no conflict
Pillow==9.2.0  # From external, no conflict
PyJWT==2.6.0  # From external, no conflict
python-dotenv==0.19.0  # From external, no conflict
python3-openid==3.2.0  # From external, no conflict
requests==2.28.1  # From external, no conflict
requests-oauthlib==1.3.1  # From external, no conflict
social-auth-app-django==5.0.0  # From external, no conflict
social-auth-core==4.3.0  # From external, no conflict
urllib3==1.26.12  # From external, no conflict

